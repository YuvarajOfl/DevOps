name: Yuvaraj-Pipeline

on:
  workflow_dispatch

jobs:
  Yuvaraj-job:
    runs-on: ubuntu-latest

    steps:
      - name: Print Yuvaraj job
        run: echo "Hello Yuvaraj"

      - name: Install Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Check Python version
        run: python --version
